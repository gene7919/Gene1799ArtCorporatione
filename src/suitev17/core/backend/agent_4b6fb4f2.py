# Auto-agent generato il 2026-02-20 22:44:04

    def load_configs(self):
                        (content, ','.join(selected), sched_dt, 'scheduled',
        try:
        for s in self.script_manager.get_scripts():

