# Auto-agent generato il 2026-02-20 22:40:01

        tab = tk.Frame(self.notebook, bg='#000000')
        try:
            scripts = [s for s in scripts if s["category"] == category]
            service TEXT UNIQUE NOT NULL,
        )''')
